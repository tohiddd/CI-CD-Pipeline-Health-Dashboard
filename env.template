# CI/CD Pipeline Health Dashboard - Environment Configuration
# Copy this file to .env and fill in your actual values

# GitHub Integration (Required - Get from https://github.com/settings/tokens)
GITHUB_TOKEN=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

# Jenkins Integration (Optional)
JENKINS_URL=http://your-jenkins-url:8080
JENKINS_USERNAME=your_jenkins_username
JENKINS_API_TOKEN=your_jenkins_api_token

# Email Alerts for Pipeline Failures (Highly Recommended)
# Configure these to receive email notifications when pipelines fail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Instructions:
# 1. For Gmail: Enable 2-factor auth, then generate an "App Password" (not your regular password)
# 2. For Outlook: Use smtp-mail.outlook.com:587
# 3. For other providers: Check their SMTP documentation
# 4. Leave empty to disable email alerts

# Slack Webhooks for Real-time Notifications (Recommended!)
# Much easier than email - just create a Slack webhook URL
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Instructions:
# 1. Go to https://api.slack.com/messaging/webhooks
# 2. Create a new Slack app or use existing workspace
# 3. Add "Incoming Webhooks" feature
# 4. Create webhook for your channel (e.g. #ci-cd-alerts)
# 5. Copy the webhook URL and paste above
# 6. Leave empty to disable Slack notifications

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this

# Development settings (leave as-is for local development)
NODE_ENV=development
DATABASE_URL=postgresql://cicd_user:cicd_password@postgres:5432/cicd_dashboard
REDIS_URL=redis://redis:6379
